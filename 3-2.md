# 환율 AI 비서 — 과제 수행 프롬프트 모음

> **주제:** 원/달러 환율(USD/KRW) 일별 종가
> **한 줄 소개:** 내 환율 데이터를 이해하고 "요즘 환율 어때?"에 실제 수치로 답하는 AI 비서
> **사용법:** ①마스터 프롬프트를 먼저 넣고 → Phase 1 결과를 직접 확인 → "다음" → ②Phase 2~9를 하나씩 붙여넣기

---

## 진행 상황

| Phase | 내용 | 상태 |
|---|---|---|
| 1 | 환경설정 · 프로젝트 구조 · provider 교체형 AI 레이어 | ✅ 완료 (`backend/`) |
| 2 | Firestore 연동 + CSV 522건 적재 | ✅ 완료 (`scripts/seed_data.py`) |
| 3 | 데이터 CRUD API | ✅ 완료 (검증 28/28 통과) |
| 4 | 요약/분석 API | ✅ 완료 (기존 분석과 14개 지표 일치, 단위 테스트 17개) |
| 5 | 대화 기록 API | ✅ 완료 (검증 41/41 통과) |
| 6 | 챗 API (컨텍스트 주입) | ⬜ |
| 7 | 프론트엔드 | ⬜ |
| 8 | 배포 (Render + Vercel) | ⬜ |
| 9 | README + 최종 점검 | ⬜ |

---

## 0. 시작 전 준비물 체크

| 준비물 | 발급처 | 비고 |
|---|---|---|
| Python 3.10+ | python.org | `python --version`으로 확인 |
| Firebase 프로젝트 + 서비스 계정 키 | console.firebase.google.com | 프로젝트 설정 → 서비스 계정 → 새 비공개 키 생성(JSON) |
| **Gemini API 키 (개발용·무료)** | aistudio.google.com/apikey | 무료 티어, 결제 수단 불필요 |
| OpenAI API 키 (제출·시연용) | platform.openai.com | 과제 요건이 GPT API이므로 최종 제출 시 필요. 사용량 한도 설정 권장 |
| GitHub 계정 | github.com | 백엔드/프론트 레포 각각 또는 모노레포 |
| Render 계정 | render.com | GitHub 연동 |
| Vercel 계정 | vercel.com | GitHub 연동 |
| **환율 원본 데이터** | `codyssey/usdkrw-exchange-rate-analysis/data/usdkrw_2024_2026.csv` | **이미 보유 (522영업일)** |

> 💡 **데이터는 새로 만들 필요 없습니다.** 기존 분석 프로젝트의 CSV(2024-09-05 ~ 2026-09-04, 522행)를 그대로 Firestore에 올리면
> "최소 100개 이상" 요건을 실데이터로 충족합니다. CSV 컬럼: `Date, rate, is_filled, ret_pct, ma5, ma20, ma60, vol20_annual_pct, ...`

---

## 1. 마스터 프롬프트 (첫 입력용)

```text
너는 시니어 풀스택 개발자이자 나의 페어 프로그래밍 파트너다.
아래 과제를 처음부터 끝까지 함께 완성한다. 내가 이해하지 못한 채 코드만 쌓이는 걸 원치 않으니,
파일을 만들 때마다 "이 파일이 왜 필요한지"를 2~3줄로 먼저 설명하고 코드를 작성해라.

# 만들 것
"내 환율 데이터를 아는 AI 비서" 웹 서비스.
원/달러 환율 시계열을 Firestore에 저장 → 통계 요약 생성 → 그 요약을 시스템 프롬프트에 주입 →
GPT가 실제 내 데이터에 근거해 답하는 챗봇. 프론트는 바닐라 HTML/CSS/JS.

# 데이터 주제 (확정)
- 항목: 원/달러 환율(USD/KRW) 일별 종가
- 단위: 원(KRW), 소수점 2자리
- 기간: 2024-09-05 ~ 2026-09-04 (영업일 기준 522개)
- 원본: 기존 분석 프로젝트의 CSV 파일
  경로: ../usdkrw-exchange-rate-analysis/data/usdkrw_2024_2026.csv
  컬럼: Date, rate, is_filled, ret_pct, iqr_outlier, suspect_tick, ma5, ma20, ma60, dev_ma20_pct, vol20_annual_pct
  ※ 파일 선두에 BOM(﻿)이 있으니 encoding="utf-8-sig"로 읽어라.
- 이 CSV의 Date를 date로, rate를 value로 매핑해 Firestore에 적재한다.

# 기술 스택 (변경 불가)
- Backend: FastAPI + uvicorn + Pydantic v2
- DB: Firebase Firestore (firebase-admin)
- AI: **provider 교체형 추상화 레이어**
  * 개발 중: Gemini 무료 티어 (gemini-3.6-flash) — 비용 0
  * 제출/시연: OpenAI GPT (gpt-4o-mini) — 과제 요건 충족
  * 환경변수 AI_PROVIDER=gemini|openai 한 줄로 전환, 코드 수정 0줄
  * 출력 토큰 500 제한으로 비용/쿼터 방어
- Frontend: HTML/CSS/JS 바닐라 (React/Vue 등 프레임워크 금지)
- 배포: Backend → Render, Frontend → Vercel
- Python 3.10+, venv

# 프로젝트 구조 (이 구조를 지켜라)
fx-ai-assistant/
├── backend/
│   ├── main.py                 # FastAPI 앱 생성, CORS, 라우터 등록, 헬스체크
│   ├── config.py               # 환경변수 로딩 (python-dotenv)
│   ├── database.py             # Firestore 초기화 (싱글톤)
│   ├── models/schemas.py       # Pydantic 요청/응답 모델
│   ├── routers/
│   │   ├── data.py             # /api/data (CRUD 4개 + summary 1개)
│   │   ├── conversations.py    # /api/conversations
│   │   └── chat.py             # /api/chat
│   ├── services/
│   │   ├── data_service.py     # Firestore CRUD 로직
│   │   ├── analysis_service.py # 통계/추세 계산 (요약 생성) — 순수 함수
│   │   └── ai_service.py       # 프롬프트 조립 + OpenAI 호출
│   ├── scripts/seed_data.py    # CSV → Firestore 적재
│   ├── requirements.txt
│   └── .env.example
├── frontend/
│   ├── index.html
│   ├── style.css
│   ├── app.js
│   ├── config.js               # API_BASE_URL 주입 지점
│   └── vercel.json
└── README.md

라우터는 HTTP 입출력만, 서비스는 비즈니스 로직만 담당한다(관심사 분리).
나중에 "왜 이렇게 나눴는지" 설명할 수 있도록 각 레이어 역할을 파일 상단 주석에 남겨라.

# API 명세 (정확히 이대로)
- POST   /api/data               body: {date:"YYYY-MM-DD", value:number, memo?:string} → 생성 문서
- GET    /api/data               query: limit, start_date, end_date (선택) → 날짜 오름차순 목록
- GET    /api/data/summary       → 아래 요약 스키마
- PUT    /api/data/{id}          → 부분 수정
- DELETE /api/data/{id}          → 삭제
- POST   /api/conversations      → 대화 저장
- GET    /api/conversations      → 목록(title/updated_at/미리보기, messages 미포함 — README에 명시)
- GET    /api/conversations/{id} → 해당 대화의 전체 messages
- DELETE /api/conversations/{id} → 삭제
- POST   /api/chat               body: {message:string, conversation_id?:string}
                                  → {reply, conversation_id}

⚠ 중요 1: /api/data/{id} 보다 /api/data/summary 를 **먼저** 선언해라.
          (안 그러면 "summary"가 {id}로 매칭돼 404/422가 난다)
⚠ 중요 2: 환율은 하루 1건이 원칙이다. POST 시 같은 date가 이미 있으면 409 Conflict를 반환하고,
          문서 ID를 date(YYYY-MM-DD)로 쓰거나 별도 검사 로직을 넣어라. 어떤 방식을 택했는지 설명해라.

# 요약(summary) 응답 스키마 — 환율 맞춤
{
  "period": "2024-09-05 ~ 2026-09-04",
  "count": 522,
  "unit": "KRW/USD",
  "metrics": {
    "average": 1431.01,
    "median": 1435.77,
    "max": 1554.48, "max_date": "2026-06-08",
    "min": 1309.30, "min_date": "2024-09-30",
    "range": 245.18,
    "std": 50.81,
    "first_rate": 1331.01,
    "last_rate": 1355.41,
    "total_change_pct": 1.83
  },
  "recent_20_avg": ...,
  "previous_20_avg": ...,
  "change_pct": ...,
  "trend": "원화 약세 (최근 20영업일 평균이 이전 20영업일 대비 +1.2%)"
}

📌 환율 데이터 특성 반영 — 반드시 지켜라:
1. **total(합계)은 넣지 마라.** 환율의 합은 의미가 없다. 대신 range(변동폭), std(변동성),
   first/last와 total_change_pct(기간 수익률)를 지표로 제공한다. 이 판단 근거를 README에 한 줄로 적어라.
2. **trend 판정 기준은 ±0.5%** 다. (환율은 일별 변동이 작아 ±5% 기준을 쓰면 항상 "유지"가 나온다)
   - change_pct > +0.5% → "원화 약세(환율 상승)"
   - change_pct < -0.5% → "원화 강세(환율 하락)"
   - 그 사이 → "보합"
3. 비교 구간은 7일이 아니라 **20영업일(약 1개월)** 로 잡아라. 주말에 데이터가 없기 때문이다.
4. trend 문자열은 하드코딩 금지. 반드시 계산 결과로 생성해라.

# 컨텍스트 주입 방식
POST /api/chat 흐름:
1) analysis_service로 요약 계산 (내부 함수 호출, 자기 자신에게 HTTP 재요청 금지)
2) 아래 시스템 프롬프트에 요약을 문자열로 삽입
3) 이전 대화 messages를 최근 10턴만 붙여 OpenAI 호출
4) user/assistant 메시지를 conversations 문서에 자동 저장
   (conversation_id 없으면 새로 생성, 첫 질문 앞 20자를 title로)

시스템 프롬프트 템플릿:
"""
당신은 사용자의 원/달러 환율 데이터 분석 비서입니다.

[사용자 데이터 요약]
- 항목: 원/달러 환율(USD/KRW) 일별 종가, 단위: 원
- 데이터 기간: {period}
- 총 레코드: {count}개 (영업일 기준)
- 평균: {average}원 / 중앙값: {median}원
- 최고: {max}원 ({max_date}) / 최저: {min}원 ({min_date})
- 변동폭: {range}원 / 표준편차: {std}원
- 기간 시작 {first_rate}원 → 최근 {last_rate}원 ({total_change_pct}%)
- 최근 추세: {trend}

[답변 규칙]
1. 위 요약에 있는 수치만 근거로 답하고, 없는 수치는 절대 지어내지 마세요.
2. 요약으로 답할 수 없으면 "기록된 데이터로는 알 수 없습니다"라고 말하세요.
3. 환율 상승 = 원화 약세, 환율 하락 = 원화 강세임을 정확히 구분해 설명하세요.
4. 미래 환율 예측이나 매수/매도 같은 투자 권유는 하지 마세요.
   추세 해석까지만 하고, 필요하면 "투자 판단은 본인 책임"임을 덧붙이세요.
5. 한국어로, 3문장 이내로 간결하게 답하세요.
"""

# 데이터 모델 (Firestore)
data 컬렉션:
  { date: "2026-09-04", value: 1355.41, memo: "FOMC 금리 동결 발표",
    created_at: <timestamp>, updated_at: <timestamp> }
conversations 컬렉션:
  { title: "이번 달 환율 어때?", messages: [{role, content, timestamp}],
    created_at: <timestamp>, updated_at: <timestamp> }

# 보안/검증 (감점 포인트이므로 반드시)
- 키는 전부 환경변수. 코드·프론트에 하드코딩 금지. .env와 서비스계정 JSON은 .gitignore.
- Pydantic v2 검증:
  * date: `^\d{4}-\d{2}-\d{2}$` 정규식 + 실제 날짜 파싱 가능 여부
  * value: float, ge=500, le=5000 (원/달러 환율의 현실적 범위)
  * memo: max_length=200, 선택
  * 각 필드에 json_schema_extra 예시값을 넣어 Swagger에서 바로 Try it out 가능하게
- 모든 라우터에 try/except + HTTPException(400/404/409/500), 에러 응답에 내부 스택 노출 금지
- CORS는 ALLOWED_ORIGINS 환경변수를 콤마로 분리해 적용 (개발 중 localhost 포함)

# 환경변수
백엔드: AI_PROVIDER, GEMINI_API_KEY, GEMINI_MODEL, OPENAI_API_KEY, OPENAI_MODEL,
        MAX_OUTPUT_TOKENS, FIREBASE_SERVICE_ACCOUNT_JSON(JSON 문자열), ALLOWED_ORIGINS
프론트: API_BASE_URL
※ Render는 키 파일 업로드가 어려우니 서비스계정 키를 **JSON 문자열 환경변수**로 받아 json.loads로 파싱하고,
   로컬에서는 파일 경로(GOOGLE_APPLICATION_CREDENTIALS)도 허용하는 폴백을 넣어라.

# 프론트엔드 요구사항 (한 페이지)
1) 채팅: 입력창, 말풍선 대화, "AI가 생각 중..." 로딩 표시, Enter 전송
2) 데이터 관리: 추가 폼(date/value/memo), 목록 테이블, 수정·삭제 버튼 동작
3) 대화 기록: 저장된 대화 목록 클릭 → 해당 대화 메시지 복원
4) 요약 카드: 기간/개수/평균/최고/최저/변동폭/추세 상시 표시
   - 추세가 "원화 약세"면 빨강, "원화 강세"면 파랑, "보합"이면 회색 배지
+ Render 무료 티어 콜드스타트 대응: 첫 로드 시 헬스체크를 호출하고
  "서버가 잠자기 상태면 첫 응답에 최대 50초 걸릴 수 있어요" 안내 배너를 표시.

# 작업 순서
Phase 1 환경설정·구조 → 2 Firestore+CSV적재 → 3 데이터 CRUD API → 4 요약/분석 →
5 대화기록 API → 6 챗 API(컨텍스트 주입) → 7 프론트 → 8 배포 → 9 README

지금은 **Phase 1만** 수행해라.
requirements.txt, .env.example, .gitignore, config.py, database.py,
main.py(헬스체크 + CORS + /docs)를 만들고,
로컬 실행 명령과 Firebase 서비스 계정 키 발급 절차를 클릭 순서까지 포함해 알려줘.
내 화면에서 동작이 확인되면 "다음"이라고 말할 테니 그때 Phase 2로 넘어가라.
```

---

## 2. 단계별 후속 프롬프트

각 Phase가 **실제로 동작하는 것을 눈으로 확인한 뒤** 다음 것을 붙여넣으세요.

### Phase 2 — Firestore 연동 + 실데이터 적재

```text
[Phase 2] Firestore 연동과 환율 데이터 적재.

1) database.py: FIREBASE_SERVICE_ACCOUNT_JSON(JSON 문자열)을 우선 사용하고,
   없으면 로컬 키 파일 경로로 폴백해 firebase-admin을 초기화해라. 중복 초기화 방지 처리 포함.

2) scripts/seed_data.py를 만들어 기존 CSV를 Firestore data 컬렉션에 적재해라.
   - 입력 경로: ../usdkrw-exchange-rate-analysis/data/usdkrw_2024_2026.csv (인자로 변경 가능)
   - encoding="utf-8-sig" (BOM 있음)
   - Date → date(문자열 YYYY-MM-DD), rate → value(소수점 2자리 반올림)
   - is_filled == True 인 행(공휴일 보간값 5건)은 memo에 "휴장일 보간값"으로 표시
   - 문서 ID는 date를 그대로 사용해 중복 적재를 원천 차단
   - Firestore batch write(500건 단위)로 나눠 쓰기 — 522건을 한 번에 커밋하면 실패한다
   - --reset 옵션: 기존 문서 전량 삭제 후 재삽입
   - --limit 옵션: 테스트용으로 N건만 적재 (처음엔 --limit 10으로 검증 후 전체 실행)
   - 실행 결과로 삽입 건수, 기간, 최초/최종 환율을 출력

3) 실행 명령과, Firebase 콘솔에서 데이터가 들어갔는지 확인하는 방법을 알려줘.
```

### Phase 3 — 데이터 CRUD API

```text
[Phase 3] 데이터 CRUD API.

models/schemas.py에 Pydantic v2로 작성:
- FxDataCreate: date(정규식+실제 날짜 검증), value(ge=500, le=5000), memo(max_length=200, optional)
- FxDataUpdate: 전 필드 Optional (부분 수정)
- FxDataResponse: id 포함
- 모든 필드에 json_schema_extra 예시값 (예: date "2026-09-04", value 1355.41, memo "FOMC 금리 동결")

services/data_service.py에 CRUD를, routers/data.py에 엔드포인트를 구현해라.
- GET /api/data: start_date/end_date/limit 필터, date 오름차순, 기본 limit 100
- POST: 같은 date 문서가 이미 있으면 409 Conflict
- PUT/DELETE: 없는 id면 404
- 라우트 선언 순서 주의 (summary가 {id}보다 위)

마지막에 curl 테스트 명령 4개(추가·조회·수정·삭제)와 /docs에서 확인할 순서를 알려줘.
```

### Phase 4 — 분석/요약 (핵심 1)

```text
[Phase 4] 환율 요약 분석.

services/analysis_service.py에 compute_summary(records: list[dict]) -> dict 를
**순수 함수**로 작성해라 (Firestore 의존 없이 리스트만 받아 계산 → 단위 테스트 가능).

계산 항목:
- period, count, unit
- metrics: average, median, max/max_date, min/min_date, range(max-min), std,
           first_rate, last_rate, total_change_pct
- recent_20_avg(최근 20영업일), previous_20_avg(그 이전 20영업일), change_pct
- trend: change_pct 기준 ±0.5%로 "원화 약세(환율 상승)" / "원화 강세(환율 하락)" / "보합"
         + 변화율을 괄호에 포함한 문장으로 생성

⚠ 합계(total)는 넣지 마라 — 환율의 합은 의미가 없다. 그 이유를 코드 주석에 남겨라.

엣지 케이스 방어: 레코드 0개, 1개, 20개 미만일 때 ZeroDivisionError/IndexError가 나지 않게
(20개 미만이면 가능한 만큼만 비교하고 trend를 "데이터 부족"으로 반환).

GET /api/data/summary 로 노출하고, 실제 응답 JSON 예시를 출력해줘.
그리고 이 요약이 위 CSV 통계(평균 1431.01, 최고 1554.48 / 2026-06-08,
최저 1309.30 / 2024-09-30)와 일치하는지 대조해줘.
```

### Phase 5 — 대화 기록 API

```text
[Phase 5] 대화 기록 API.

conversations 컬렉션에 대해 구현:
- POST /api/conversations       : {title?, messages[]} 저장
- GET  /api/conversations       : 목록 — title, updated_at, 마지막 메시지 미리보기(40자)만 반환.
                                  messages는 포함하지 않는다.
- GET  /api/conversations/{id}  : 전체 messages 포함
- DELETE /api/conversations/{id}: 삭제 (없으면 404)

목록은 updated_at 내림차순 정렬.
"목록 응답에 messages를 넣지 않은 이유(페이로드 절감 + 목록 조회 속도)"를
README에 그대로 붙일 수 있게 2문장으로 정리해줘.
```

### Phase 6 — AI 챗 API (핵심 2)

```text
[Phase 6] AI 챗 API — 컨텍스트 주입.

POST /api/chat 구현:
① analysis_service.compute_summary로 요약 계산 (내부 함수 호출, self-HTTP 금지)
② 마스터 프롬프트의 시스템 프롬프트 템플릿에 요약 값 주입
③ conversation_id가 있으면 기존 messages 최근 10턴을 context로 포함
④ services/ai_service.py 의 generate_reply(system_prompt, messages) 호출
   (이미 Phase 1에서 provider 교체형으로 구현됨 — Gemini/OpenAI 자동 분기)
⑤ user/assistant 메시지를 conversations에 자동 저장하고 {reply, conversation_id} 반환

예외 처리: ai_service.AIServiceError 를 잡아 exc.status_code / exc.message 를 그대로
HTTPException 으로 변환해라. 키 오류·쿼터 초과·타임아웃은 ai_service 가 이미
한국어 메시지로 번역해 두었으므로 라우터에서 중복 처리하지 마라.

구현 후 아래 3개 질문으로 검증하고, 답변에 실제 수치가 들어가는지 확인해줘:
1. "최근 환율 추세가 어때?"       → trend와 변화율이 인용되어야 함
2. "가장 환율이 높았던 날은?"      → 1554.48원, 2026-06-08이 나와야 함
3. "내년 환율 얼마까지 갈까?"      → 예측을 거부하고 추세 해석만 해야 함 (규칙 4 검증)

수치가 틀리게 나오면 프롬프트 어느 부분을 고쳐야 하는지도 설명해줘.
```

### Phase 7 — 프론트엔드 (바닐라)

```text
[Phase 7] 프론트엔드 — HTML/CSS/JS 바닐라. 프레임워크 금지.

파일: index.html / style.css / app.js / config.js / vercel.json

레이아웃(데스크톱 기준 3분할, 모바일 400px에서 세로 스택):
- 상단: 요약 카드 바 — 기간 / 레코드 수 / 평균 / 최고(날짜) / 최저(날짜) / 변동폭 / 추세 배지
  추세 배지 색: 원화 약세=빨강, 원화 강세=파랑, 보합=회색
- 좌: 대화 기록 목록 (클릭 → GET /api/conversations/{id}로 메시지 복원, 삭제 버튼, 새 대화 버튼)
- 중앙: 채팅 — 말풍선, Enter 전송, 전송 중 버튼 비활성화 + 타이핑 인디케이터, 에러는 빨간 말풍선
- 우: 데이터 관리 — 추가 폼(date/value/memo) + 최근 20건 테이블 + 행별 수정/삭제 버튼

기타 필수:
- config.js가 window.API_BASE_URL을 정의 (Vercel 환경변수로 치환 가능한 구조)
- 첫 로드 시 /health 호출 + "서버 절전 해제 중, 최대 50초 걸릴 수 있어요" 안내 배너
  (응답 오면 배너 자동 숨김)
- XSS 방지: innerHTML 대신 textContent 사용
- 모든 fetch를 try/catch로 감싸고 네트워크 오류 시 사용자에게 안내
- CSS 변수 기반 디자인, 깔끔한 카드 UI, 가로 스크롤 없이 반응형
```

### Phase 8 — 배포

```text
[Phase 8] 배포.

1) .gitignore 점검 (.env, *serviceAccount*.json, __pycache__, venv/) 후 GitHub 푸시 절차
   ※ 이미 커밋된 키가 없는지 git log -p로 확인하는 방법도 알려줘
2) Render Web Service:
   - Root Directory: backend
   - Build Command: pip install -r requirements.txt
   - Start Command: uvicorn main:app --host 0.0.0.0 --port $PORT
   - 등록할 환경변수 목록과 각각의 값 형식
3) FIREBASE_SERVICE_ACCOUNT_JSON을 Render에 넣을 때 private_key의 \n 처리 주의사항과 해결법
4) Vercel 배포: frontend를 Root Directory로, vercel.json 설정, API_BASE_URL 주입 방법
5) 배포 후 Render의 ALLOWED_ORIGINS에 Vercel 도메인을 추가하고 재배포 → CORS 재확인

각 단계를 UI 클릭 순서까지 포함해 알려주고,
흔한 실패 원인 체크리스트(CORS 에러, 502 Bad Gateway, 키 파싱 실패, /docs 404)와
각각의 해결법을 표로 정리해줘.
```

### Phase 9 — README + 최종 점검

```text
[Phase 9] README.md 작성.

포함할 것:
- 서비스 소개: "환율 데이터를 아는 AI 비서" — 어떤 문제를 푸는지
- 기술 스택
- 아키텍처 다이어그램 (텍스트/mermaid)
- 배포 URL 3개 (프론트 / 백엔드 API / Swagger)
- 로컬 실행 방법 (백엔드 + 프론트)
- 환경 변수 표 (이름 · 설명 · 예시값)
- API 명세 표
- Firestore 컬렉션 구조
- **"데이터 요약을 시스템 프롬프트에 주입하는 원리" 설명 섹션** (과제 목표 항목)
- 설계 결정 기록:
  * 라우터/서비스 분리 기준
  * Pydantic 검증을 적용한 이유
  * 환율 데이터라서 total 대신 range/std/변화율을 쓴 이유
  * trend 판정 기준을 ±0.5%, 20영업일로 잡은 이유
  * 대화 목록 응답에 messages를 넣지 않은 이유
- Render 콜드스타트 안내
- 스크린샷 자리표시자 3개 (채팅 / 데이터관리 / 대화기록)

마지막에 과제 요구사항 1~10번 기준으로 제출 전 최종 점검 체크리스트를 만들어줘.
```

---

## 3. 보너스 과제 프롬프트 (선택)

```text
[보너스 A] Function Calling + 멀티채널.

/api/chat을 tool-calling 루프로 확장해라. 정의할 도구:
- get_fx_summary()                        : 전체 요약
- get_rates_by_period(start, end)         : 특정 기간 환율 조회
- get_extreme_days(direction, n)          : 최고/최저 상위 N일
- search_conversations(keyword)           : 과거 대화 검색

"작년 9월엔 환율이 얼마였어?" 같은 질문에서 GPT가 get_rates_by_period를 호출하게 하고,
응답에 tool_calls trace(어떤 근거로 어떤 도구를 왜 호출했는지)를 함께 반환해라.

그리고 동일 기능을 GPT Actions용 OpenAPI 스펙 또는 MCP Server 중 하나로 외부 노출하고,
외부 클라이언트에서 호출되는 것까지 검증해라.
README에 호출 흐름 다이어그램(사용자 → GPT → 도구 선택 → API → 응답 재조합)을 넣어줘.
```

```text
[보너스 B] 시각화 + 내보내기 + 다크모드.

1) GET /api/data/statistics 추가:
   - 5/20/60일 이동평균 시계열
   - 20일 연율화 변동성(vol20_annual_pct)
   - 월별 평균 환율
   - 전월 대비 증감률
   (기존 CSV에 ma5/ma20/ma60/vol20_annual_pct 컬럼이 이미 있으니 로직 검증에 활용)

2) 프론트에 Chart.js(CDN)로 라인 차트: 일별 환율 + 20일 이동평균 오버레이,
   최고점/최저점 마커 표시, 기간 선택(1개월/3개월/1년/전체) 토글

3) CSV / JSON 내보내기 버튼 (현재 필터 조건 그대로 다운로드)

4) 다크모드 토글: CSS 변수 교체 방식, 선택값 localStorage 저장, 새로고침해도 유지
```

---

## 4. 제출용 스크린샷 3장 체크

| # | 화면 | 반드시 보여야 할 것 |
|---|---|---|
| 1 | 채팅 | 상단 요약 카드(기간·개수·추세) + 내 질문 + 실제 수치가 들어간 AI 답변 |
| 2 | 데이터 관리 | 추가 or 수정 or 삭제 동작이 목록에 반영된 상태 |
| 3 | 대화 기록 | 목록에서 이전 대화를 클릭해 메시지가 복원된 상태 |

---

## 5. 진행 팁

- **Phase를 건너뛰지 마세요.** 한 번에 다 만들면 Firestore 인증 오류인지 CORS인지 라우트 순서인지 구분이 안 됩니다.
- **비용 방어:** 개발 내내 `AI_PROVIDER=gemini`(무료)로 두고, 최종 검증·시연 직전에만 `openai`로 바꾸세요. OpenAI 대시보드에서 월 사용 한도도 미리 걸어두면 안전합니다. `MAX_OUTPUT_TOKENS=500`은 그대로 유지하세요.
- **적재는 `--limit 10`부터.** 522건을 바로 넣지 말고 10건으로 스키마를 확인한 뒤 `--reset`으로 전체 적재하세요.
- **과제 목표의 "설명할 수 있어야 한다"** 대비: Phase 3·4·6이 끝날 때마다 아래를 추가로 던지세요.

```text
방금 만든 코드 기준으로 아래 3개를 각각 5문장으로 설명해줘.
(1) 라우터와 서비스를 나눈 기준
(2) Pydantic 검증을 넣은 이유와 방식
(3) 데이터 요약을 시스템 프롬프트에 주입하는 방식이 왜 효과가 있는지
```
